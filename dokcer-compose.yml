version: 3.0
services:
  web:
    build:
      context: ./Webapp
      dockerfile: Dockerfile
    ports:
      - "9000:9000"
    volumes:
      - ./Webapp:
  nginx:
    image: nginx:latest
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf